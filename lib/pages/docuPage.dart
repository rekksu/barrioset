// ignore_for_file: prefer_const_constructors

import 'package:flutter/material.dart';

class UserDocu extends StatelessWidget {
  const UserDocu({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
          'Documents Page',
          style: TextStyle(fontSize: 30),
        ),
      );
  }
}