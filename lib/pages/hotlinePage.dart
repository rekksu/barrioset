import 'package:flutter/material.dart';

class UserHotline extends StatelessWidget {
  const UserHotline({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
          'Emergency Hotlines Page',
          style: TextStyle(fontSize: 30),
        ),
      );
  }
}